---
import SwiperWithProps from './SwiperWithProps';

import { extractFileName} from '../utils'; 

// Lookup all images in public/images folder and extract the filenames
const allImages = await Astro.glob('../../public/images/*.jpg');
const names = allImages.map(img => extractFileName(img.default.src));
---
<SwiperWithProps images={{names}}‚ client:load />


